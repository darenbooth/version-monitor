services:
  version-monitor:
    image: darenbooth/version-monitor:latest
    container_name: version-monitor
    ports:
      - "8136:80"
    env_file:
      - .env
    volumes:
      - ./monitor_data:/app/data
    restart: always
